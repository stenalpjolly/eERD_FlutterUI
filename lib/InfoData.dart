import 'package:flutter/cupertino.dart';

class InfoData {
  String topTitle;
  String btnText;
  Color btnColor;
  Color backgroundColor;
  Color topTitleColor;
  Color btnTextColor;

  InfoData(this.topTitle, this.btnText, this.btnColor, this.backgroundColor,
      this.topTitleColor, this.btnTextColor);
}

